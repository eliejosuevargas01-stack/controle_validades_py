import { describe, test } from 'vitest';

describe('Button Component', () => {
  test.todo('deve renderizar o botão com o texto correto (children)');
  test.todo('deve chamar a função onClick ao ser clicado');
  test.todo('deve aplicar as classes CSS corretas dependendo da variante (ex: primary, danger)');
  test.todo('deve ficar desabilitado quando a prop disabled for true');
});
