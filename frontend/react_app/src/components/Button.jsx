/*
 * Arquivo: Button.jsx
 * Objetivo: Componente reutilizável de botão.
 *
 * O que fazer aqui:
 * - Criar e exportar uma função de componente React (ex: `const Button = ({ children, onClick, ...props }) => ...`).
 * - O componente deve retornar um elemento `<button>` estilizado.
 * - Receber `props` para customização (ex: variantes como 'primary', 'secondary', 'danger', tamanho, etc).
 * - Utilizar as classes de CSS ou styled-components que serão definidas depois.
 */
