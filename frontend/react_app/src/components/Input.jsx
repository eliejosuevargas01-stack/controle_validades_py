/*
 * Arquivo: Input.jsx
 * Objetivo: Componente reutilizável para campos de entrada de texto.
 *
 * O que fazer aqui:
 * - Criar e exportar o componente `Input`.
 * - Este componente deve englobar a tag `<input>` e possivelmente um `<label>` e exibição de mensagens de erro.
 * - Receber propriedades como `value`, `onChange`, `placeholder`, `type`, e `errorMessage`.
 */
